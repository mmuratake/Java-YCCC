package edu.yccc.cis174.mmuratake.Notes;

public class ErrorHandling {
	
	//See Class ReadFile under package FileIO.
	
	
	/**
	 * Try Catch:
	 * 
	 * Let me try this; if it fails, then tell the user what went wrong.
	 * 
	 */
	// Catch what you think might go wrong. (Ex: FileNotFoundException e)
	// When you catch, ALWAYS have it print the stack trace. (e.printStackTrace();)
	
	/**
	 * Throw:
	 * 
	 * Ehh, let me just shove my problems over to another person.
	 * 
	 */
	// But SOMEONE has to catch it. Preferably the one closest to the user.

}
